spring.application.name=DiagramGeneratorService
server.port=8084

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/astyann_diagrams?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# File storage
app.upload-dir=uploads/diagrams

# Retry sweep for diagrams left APPROVED without a VersionService snapshot (createSnapshot failed)
diagram.snapshot.retry.initial-delay-ms=60000
diagram.snapshot.retry.fixed-delay-ms=60000

# Downstream service URLs
services.ai.url=http://localhost:8089
services.rag.url=http://localhost:8090
services.requirement.url=http://localhost:8083
services.kroki.url=https://kroki.io
services.version.url=http://localhost:8087

# Feign timeouts (Ollama inference can be slow — unbounded, matches RequirementService)
spring.cloud.openfeign.client.config.default.connect-timeout=0
spring.cloud.openfeign.client.config.default.read-timeout=0
spring.cloud.openfeign.client.config.ai-service.read-timeout=0

logging.level.root=INFO
logging.level.afb.astyann.diagramgeneratorservice=DEBUG
