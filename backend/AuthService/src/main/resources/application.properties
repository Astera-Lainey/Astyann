spring.application.name=AuthService

server.port= 8081

## MySQL database connection
spring.datasource.url=jdbc:mysql://localhost:3307/astyann_users?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=ISI3Eng!2025
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate settings
# 'update' is safe for development: creates/updates tables automatically
spring.jpa.hibernate.ddl-auto=update
# Show and format SQL in logs (helpful for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# MySQL dialect setting
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# Performance settings
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.open-in-view=false
# Initialize database with data.sql (set to 'always' to run every time)
spring.sql.init.mode=never
spring.sql.init.platform=mysql

spring.mail.host= smtp.gmail.com
spring.mail.port= 587
spring.mail.username= simoyann2004@gmail.com
# One more thing to verify: Your Gmail password on line 29 (SkYj 30032004) ?                         ?
#     if you have 2FA enabled on that Gmail account (likely), you need to use a     ?? ???? ???? ???? ???? ????
#     Gmail App Password (https://support.google.com/accounts/answer/185833)        ?? ?  ? ?    ?  ? ?  ? ????
#     instead of your regular account password. The TLS handshake failure occurs    ?? ???? ???? ???? ???? ????
#     before authentication, so this isn't the cause of the current error, but it
#     will fail on the next step after TLS is established.
spring.mail.password= ntdb pxvh vnup pyrg
spring.mail.properties.mail.smtp.auth= true
spring.mail.properties.mail.smtp.starttls.enable= true
spring.mail.properties.mail.smtp.ssl.trust= smtp.gmail.com


# ======================
# LOGGING CONFIGURATION
# ======================
# Logging Levels (ordered from least to most verbose):
# 1. OFF     - No logging
# 2. FATAL   - Severe errors that cause premature termination
# 3. ERROR   - Runtime errors/exceptional conditions
# 4. WARN    - Undesirable but not wrong situations
# 5. INFO    - Important runtime events (startup/shutdown)
# 6. DEBUG   - Detailed flow information
# 7. TRACE   - Most detailed information (method entry/exit)

logging.level.root=INFO
# Application-specific package (update with your actual package structure)
logging.level.afb.astyann.authservice.=DEBUG
# Framework logging
logging.level.org.springframework=INFO
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=INFO
logging.level.org.hibernate=WARN
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE


# JWT Config
jwt.secret=8n3hn46GgLZa4NvXevGPDP3MrllFwTzBFO7Pp3rSMwat+jPrQFlJERVZVguJlC58hgEIWmZHCqAR4EZ+vzm0Uw==
# 2 hours
jwt.access-expiration-ms= 7200000
# 7 days
jwt.refresh-expiration-ms= 604800000 

app.frontend-url= http://localhost:4200
